-- use following to get all tables needs to truncate
select 'Truncate table'+'['+SCHEMA_NAME(SCHEMA_ID)+'].['+name+']' from sys.tables


-- To find the Foreign Key Constraints on a table, you can use below statement. manually grab [SalesLT].[Address]

SELECT * 
FROM sys.foreign_keys
WHERE referenced_object_id = object_id('[SalesLT].[Address]')


-- The below script can be used to generate drop Constraint statement for your table, manully grabl [SalesLT].[Address]

SELECT 
    'ALTER TABLE ' 
    +  OBJECT_SCHEMA_NAME(parent_object_id) 
    +'.[' + OBJECT_NAME(parent_object_id) 
    +'] DROP CONSTRAINT ' 
    + name as DropFKConstraint
FROM sys.foreign_keys
WHERE referenced_object_id = object_id('[SalesLT].[Address]') 

-- use the alter table which generated by the above script
-- truncate the above table
-- recreate constraint using the [SalesLT].[Address] keys ---

